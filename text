taskkill /F /IM "Docker Desktop.exe" 2>$null
taskkill /F /IM "com.docker.backend.exe" 2>$null
Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe" -Verb RunAs



docker build -t vue-frontend .
docker stop vue_frontend
docker rm vue_frontend
docker run -d --name vue_frontend -p 8080:80 vue-frontend


PC
 │
 ├─ edit code
 ├─ git add .
 ├─ git commit
 └─ git push
        │
        ▼
DigitalOcean
 │
 ├─ git pull
 ├─ docker build -t vue-frontend .
 └─ restart frontend container


https://www.youtube.com/watch?v=pTFZFxd4hOI&t=2s
https://www.youtube.com/watch?v=pg19Z8LL06w
https://github.com/Dan10532/vue-app/blob/master/Dockerfile
https://docs.google.com/presentation/d/1PTj7kkfIAFILY8iJoB_UdEgvAK7fXyJGoF0zb-KuVsg/edit?slide=id.gb4c4d5d5dd_0_0#slide=id.gb4c4d5d5dd_0_0
https://chat.deepseek.com/share/d02t3wbr8dkgw23kkb

docker
nginx
letsencript
linux 

docker build -t vue-pos .
Run your vue app container. => docker run -d -p 8080:80 --name vue-app vue-pos

-d means the container will run in the background
-p means port outsideport:insideport(nginx-80) 
--name means the container name
Finally write the image name

docker run -d --name my-postgres -e POSTGRES_PASSWORD=mysecretpassword -p 5432:5432  postgres
<!-- docker run -d --name my-postgres -e POSTGRES_PASSWORD=mysecretpassword -p 5432:5432 postgres -->
docker network create my_net
docker run --name my-postgres --network  my_net -e POSTGRES_PASSWORD=mysecretpassword -p 5432:5432  postgres

docker run -d --network my_net -p 8080:80 --name frontend vue-app(finally run)

Check if it's running => docker ps
View container logs => docker logs vue-app
Stop the container => docker stop vue-app
Start it again => docker start vue-app
Remove the container (after stopping it) => docker rm vue-app
Build again with a different name => docker build -t vue-pos-v2 .
Run with different port (e.g., port 3000) => docker run -d -p 3000:80 --name vue-app-2 vue-pos

        Push to Docker Hub
Tag your image => docker tag vue-pos yourusername/vue-pos:latest   
Login to Docker Hub => docker login     
Push to Docker Hub => docker push yourusername/vue-pos:latest

docker run -d --network my_net -p 8000:8000 --name backend fastapi-app
docker run -d --network my_net -p 8080:80 --name frontend vue-app

docker run -d --name backend --network my_net -p 8000:8000 -e DATABASE_URL=postgresql://postgres:12039@my-postgres:5432/vue fastapi-app uvicorn main:app --host 0.0.0.0 --port 8000


sudo apt install nginx -y
sudo systemctl start nginx
sudo ln -s /etc/nginx/sites-available/amarahaus.co.ke /etc/nginx/sites-enabled/

server {
 server_name amarahaus.co.ke;

 location / {
 proxy_pass http://207.154.206.4:8080:
 }
}

sudo systemctl reload nginx
sudo certbot --nginx -d amarahaus.co.ke -d www.amarahaus.co.ke
sudo apt update
sudo apt install certbot python3-certbot-nginx -y